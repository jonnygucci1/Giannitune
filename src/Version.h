#pragma once

namespace gianni
{
    inline constexpr const char* kVersion = "1.2.1";
    inline constexpr const char* kBuildNote = "Latency −6ms (split off) + presets removed + dynamic meta display";
}
